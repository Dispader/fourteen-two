package schedule

import spock.lang.Specification

class UPLScheduleSpec extends Specification {

    def 'the first test runs'() {
        when: 'we we do stuff'
        then: 'stuff happens'
            true
    }

}
